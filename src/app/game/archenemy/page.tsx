import { pageMetadata } from "~/config/metadata";
import ArchenemyPage from "../../../components/ArchenemyGame";

export const metadata = pageMetadata.archenemyGame;

export default function Archenemy() {
  return <ArchenemyPage />;
}
